# Clarity Backend

Backend workspace for future API development.

Current starter:

- `src/server.ts` native Node HTTP server
- `GET /health` health check
- default port `4000`

Run from the project root:

```bash
npm run dev:backend
```
